/* -----------------------------------------------------

                IREP QUIZ SETUP

-------------------------------------------------------- */

/* Empty quiz because it's not needed for Labyrinth minigames */

var IREP = IREP || {};

IREP.Quiz = (function (global, undefined) {
    
    var Quiz = function () {
        // deleted
    };

    return Quiz;
}(this));